/**
 * shared: other modules set this
 * setup: load keyframes in animations
 * create: generate boards shown, adds graphics listners
 * destroy: remove all running animations
 * data: shared animation information
 * utils: core, animations, misc (grid)
 */
tetresse.modules.tetriaGraphics = {
    shared: {},
    setup() {
        
    },
    create(game, settings) {
        
    },
    destroy(game) {
        
    },
    data: {
        
    },
    utils: {
        
    }
}